<%= form_with(model: grupo) do |form| %>
  <% if grupo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(grupo.errors.count, "error") %> impidieron guardar el grupo:</h2>
      <ul>
        <% grupo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, "Nombre del grupo" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
